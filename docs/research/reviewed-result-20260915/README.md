# Canonicalを保持し、白紙Review XDWからReviewed Resultを作るための調査

調査日：2026-09-15。**設計調査であり、新機能の実装仕様・実装完了記録ではない。**

調査時は使い捨て文書で検証し、製品の実装・公開物を変更していない。後日の参照用にこの文書と検証JSONを開発リポジトリへ収録した。SDK本文、DLL、XDW原本、ローカルパスを含む元ログは収録していない。

[調査案内](../README.md) / [文書案内](../../README.md) / [検証記録](evidence/verification-summary.json)

調査基準は下表のローカルコミット。収録先は開発mainの `8f8d53765b4ba237268aa942fef32353f90fa7c9` を基点とした文書追加であり、調査ブランチのOCR修正を併せて取り込むものではない。コードリンクはリポジトリ相対であり、後日の実装変更後は調査基準と区別して読む。

## 1. 結論

この方式に必要な基礎操作は、現行Coreと公式SDKの組み合わせで実現できる見込みが高い。白紙の複数ページ作成、文字と任意の由来情報の保存、編集後の文字・位置・サイズの読取を実機で確認した。

既存の `read_review_edits()` は流用しない。現在は「原本に重ねた1ページの対象領域について、構造を維持して文字を訂正する」機能であり、追加・削除を拒否する。新機能は全ページ・全テキストの現在の状態を読む独立した入口にする。既存APIの規則を緩めて互換性を崩す必要はない。

実装前に押さえるべき点は次のとおり。

1. 文書・ページの識別を必須にし、元OCR領域の識別は任意の来歴にする。
2. 文字が0件でもページを残す。全削除後もページ属性で元文書と対応できる。
3. Viewerでコピーすると元OCR領域IDも複製される実例がある。重複を1対1の対応と誤認しない。
4. グループ・付箋の子の座標は親からの相対座標。ページ座標へ変換する。
5. 位置・サイズは保存後に再取得する。OCRの矩形や、生成時に指定したサイズで代用しない。
6. 将来の移動・サイズ変更の判定には、Canonicalだけでなく、生成直後のReview XDWの実測情報も残す。

## 2. 調査した実装と環境

| 対象 | 確認内容 |
|---|---|
| 開発ソース | `gogobousousannrinnsha/dw-ocr` の調査時ローカルチェックアウト |
| 調査基準コミット | `0e93209f99daea0d8497687d728f8629a77f927c` |
| Python | 3.13.15、Windows x64 |
| 実機DLL | `C:\Windows\System32\xdwapi.dll` |
| API応答の版 | 10.1.1 |
| DLLファイル版 | 10.1.0.17。API応答の版とは別項目 |
| 公式仕様 | SDK 9.1.7の日本語 `XDWAPI.xdw`、ヘッダー `xdw_api.h` |
| 実機確認範囲 | このホストの上記DLL。DocuWorks 9.1実行環境での新方式の確認ではない |

環境の詳細とSDKファイルのSHA-256は[environment.json](evidence/environment.json)に記録した。SDKは読み取り専用で開き、抽出前後のハッシュ一致を確認した。第三者ラッパーの説明は根拠にしていない。

### 既存機能の再利用と変更点

| 部分 | 現在の挙動 | 今回の設計への反映 |
|---|---|---|
| Canonicalの保存・読込 | 元文書、ページ画像、認識結果等をハッシュ付きで保持。1.0/1.1対応 | 原記録の保存と読込を再利用。形式変更は不要 |
| Canonical 1.1の空ページ | `NO_TEXT_DETECTED` と空のregionsを扱える | 空ページも白紙XDWへ出力する |
| 既存Review生成 | 原本の1ページを抽出して赤文字を重ねる | 白紙生成と全ページ構成を新設 |
| 既存Review識別 | `DW-OCR.ReviewIdentity` にrun・review・region・manifest情報を格納 | 設計を参考に、新方式専用の識別形式を設ける |
| 既存Review読取 | 1ページ限定、属性のある文字だけを読む。件数とID集合を厳密照合 | 無属性の新規文字も読む独立処理が必要 |
| 既存訂正適用 | 元領域の位置・順序・信頼度を保持 | 自由編集フローではCorrectionSetへの変換を挟まない |
| 保存処理 | 一時フォルダーから新規保存先へrenameして確定 | 同一ボリュームの文書単位確定に再利用できる |
| OCRの起動 | BAT → `start_ocr.py` → `process_documents()` | OCR成功後のReview生成工程を追加する |
| 取り込み起動 | 今回の自由編集用入口はない | 別BATとPython入口を追加する |

現在の `start_ocr.py` は引数のパス、またはINPUTフォルダーを使う。前の会話で挙げた入力・出力フォルダーの選択画面は新UIの案であり、現行機能ではない。

### 主なコード根拠

- [Canonicalの型・検証](../../../packages/docuworks-integrations/docuworks_integrations/results.py#L69)
- [Canonical読込と元bundleの検証](../../../packages/docuworks-integrations/docuworks_integrations/results.py#L179)
- [現行Review読取のnative境界](../../../packages/docuworks-integrations/docuworks_integrations/review_xdw.py#L105)
- [現行の複数領域照合](../../../packages/docuworks-integrations/docuworks_integrations/review_xdw.py#L281)
- [現行の複数領域生成](../../../packages/docuworks-integrations/docuworks_integrations/review_xdw.py#L307)
- [現行の訂正候補作成](../../../packages/docuworks-integrations/docuworks_integrations/review_xdw.py#L386)
- [Coreのアノテーション座標・サイズ](../../../packages/docuworks-ctypes/docuworks_ctypes/document.py#L578)
- [Coreのユーザー定義属性](../../../packages/docuworks-ctypes/docuworks_ctypes/document.py#L690)
- [OCRジョブ工程](../../../packages/docuworks-integrations/docuworks_integrations/jobs.py#L22)
- [一時保存と新規フォルダー確定](../../../packages/docuworks-integrations/docuworks_integrations/_storage.py#L28)

## 3. 公式SDKで確認した仕様

ページ番号は「XDWファイル内のページ / 本文に印刷されたページ」の順に記す。

| 目的 | API・仕様 | 根拠ページ |
|---|---|---|
| 白紙ページ作成 | 白いBMPを `XDW_CreateXdwFromImageFileW` で取り込み、`XDW_CREATE_USERDEF` で用紙寸法を指定 | 49–51 / 43–45 |
| 複数ページ化 | `XDW_InsertDocumentW` で生成済みページを挿入 | 158 / 152 |
| 文書識別 | `XDW_SetUserAttribute` / `XDW_GetUserAttribute` | 208 / 202、154 / 148 |
| ページ識別 | `XDW_SetPageUserAttribute` / `XDW_GetPageUserAttribute` | 207 / 201、140 / 134 |
| 元OCR領域の来歴 | `XDW_SetAnnotationUserAttribute` / `XDW_GetAnnotationUserAttribute` | 196 / 190、80 / 74 |
| ページ情報 | `XDW_GetPageInformation`。拡張構造体にはページ回転角がある | 133–135 / 127–129 |
| 種類・位置・サイズ・子の数 | `XDW_GetAnnotationInformation`。長さの単位は1/100mm | 78–79 / 72–73 |
| Unicode文字列 | テキスト標準属性 `%Text` をW系APIで取得 | 73 / 67、223 / 217 |
| 回転・縦書き | `%TextOrientation` は時計回り0～359度、`%TextDirection` は横0・縦1 | 223 / 217 |
| 付箋上の座標 | 親の左上が原点 | 22–23 / 16–17 |
| テキスト枠のサイズ変更 | 折り返しONかつ回転0度が条件 | 195 / 189 |

属性名は255バイトまで。値は任意のバイト列として保存できる。属性が存在しない場合は取得エラーとなるが、0バイトの値が存在する場合もある。この2つは区別する。現行DLLでは、存在しない属性の初回サイズ照会が `XDW_E_INVALIDARG` になることを既存実装で扱っている。その他のSDKエラーまで「来歴なし」に置換してはならない。

白紙生成の試作はユーザー所有の白紙XDWを雛形にせず、合成した白BMPを使用した。これは白い画像を持つXDWであり、Viewerの「新規作成」でできる `XDW_PGT_NULL` と同一のページ種別ではない。今回の文字校正用途に必要なページ寸法・編集可能な注釈は実機確認できたが、新規Viewer表示の見た目は未確認。

公式Viewerヘルプでも、テキストの折り返し設定により枠サイズ変更の可否が変わり、回転・縦書きなどを編集できることを確認した。
[DocuWorks 10「テキスト」を使用する](https://opencds-fb.fujifilm.com/gen/docuworks_aux/v10/help/jpn/viewer/3_03_02_01.html)
／[DocuWorks 9.1「テキスト」を使用する](https://opencds-fb.fujifilm.com/gen/docuworks_aux/v91/help/jpn/viewer/3_03_02_01.html)

## 4. 今回の実機検証結果

実OCRは実行せず、合成文書を生成してSDKで編集・保存・閉じる・再読込した。基準文書のハッシュを前後で照合した。新しいViewer操作は行っていない。

| 検証 | 結果 | 実装への影響 |
|---|---|---|
| A4・任意寸法・A3横の3ページ | 210×297、148.37×209.83、420×297mmを正確に取得 | 用紙寸法を明示した白紙生成が可能 |
| 文字なしページ | 2ページを文字0件のまま保持 | 空ページを省略しない |
| 文書・ページ属性 | 保存・再読込、ページ挿入後も取得 | 文書/ページ対応の基礎になる |
| 日本語・改行・前後空白・補助漢字・絵文字 | 試験文字列を完全一致で取得 | JSONもUTF-8で保存し、勝手にtrim/正規化しない |
| 文字修正と移動 | 33.33、44.44mmへ移動した値と元領域IDを取得 | 現在位置を採用できる |
| 属性なし・黒文字の追加 | 新規文字を取得 | 色や元領域IDで取り込み対象を絞らない |
| 文字削除・非テキスト追加 | 残ったテキスト3件と矩形1件を区別 | 非テキストは結果対象外にできる |
| 元領域IDの重複 | 同じ由来の文字2件を両方取得 | 来歴を曖昧とし、文字自体を捨てない |
| 元領域属性の欠落・不正JSON | 文字の取得は可能 | 任意来歴の問題と文書取込失敗を分ける |
| 全テキスト削除 | 文字0件でもページ属性は保持 | 「空になったページ」を表現できる |
| 空文字・空白だけの文字 | APIではそのまま存在・取得可能 | 削除とは別。扱いを仕様化する |
| 30度回転 | 外接矩形と回転角を取得 | 元のOCR polygonは流用しない |
| テキスト高さ | 指定10/20/30mmに対し、実測11.86/20.32/33.01mm | 指定サイズを結果に書かず、保存後の実測値を使う |
| グループ内文字 | 領域ID保持。座標は親グループからの相対値 | 親階層を保持してページ座標へ変換 |
| 付箋内文字 | 親(100,100)mm、子(2,3)mm。親移動後も子の相対値は不変 | ページ上では(102,103)mm。親移動後は加算し直す |
| 縦書き | 方向属性と文字内容を取得 | 向きを保存し、並び順推定と分離 |
| ページコピー | コピー前と同じページIDが残る | 重複ページIDを検出する |
| ページ180度回転 | 寸法とページIDは不変、拡張情報の角度は180 | 寸法・IDだけの照合では不足 |

高さの差の発生原因そのものは断定していない。再取得したSDKの生の寸法ですでに差があり、保存後も同じ値だった。フォントや行間等の影響を含む可能性があるため、今回の結果を全環境共通の補正式として使わない。

### 過去のViewer保存文書を今回再読込した結果

既存のViewer検証6ファイルを読み取り専用で開き、過去の記録とSHA-256が一致すること、今回の読取前後でも不変であることを確認した。

- 文字編集・移動・未変更では、元領域IDが残っていた。
- コピーケースでは、2つのテキストに**同じ元領域ID**が残っていた。
- 削除ケースではテキストが0件になっていた。
- 空白ケースでは、文字領域と元領域IDが残っていた。

これはユーザーが過去にViewerで操作・保存した文書の今回の再検証である。今回生成した白紙・複数ページ・新しい文書/ページ属性について、Viewerでの保持を保証する証拠にはしない。

### 検証記録

- [初回検証：9件成功、2件は追加調査](evidence/probe-200702-bed467/results.json)
- [サイズ差と過去Viewer保存文書の確認](evidence/probe-200828-e6e89d/results.json)
- [付箋・ページコピー・縦書きの追加確認](evidence/probe-200849-5b9c69/results.json)
- [180度ページ回転の確認](evidence/probe-201003-5a8e3f/results.json)

最初の付箋試験は検証コードの引数指定ミス、追加試験の一部は検証コードの変数参照ミスで実行に至らなかった。検証コードだけを修正し、該当ケースを実施した。高さ20mmの完全一致という初回の仮説は成立しなかったため、これを成功扱いに置き換えず、実測値の観測結果として残した。製品の不具合修正はしていない。

## 5. 提案するデータ構造

以下は実装案であり、既存API・確定済みの新形式ではない。

### 5.1 Canonical

形式1.0/1.1と保存bundleを維持する。既存の `confidence` は既にnullを許容するため、「confidenceが入れられない」ことだけが分離理由ではない。固定的な領域ID・四角形・px/mm整合・空文字不可など、機械OCRの検証規則を自由編集へ持ち込まないことが主な理由である。

新規の標準OCRフローは全ページが揃ったCanonical 1.1を使うのが自然。旧bundleや一部ページだけのOCR結果は、ページ集合と元文書ページ数を明示検証する。未処理ページを「認識文字なし」とみなして白紙で補完しない。

### 5.2 Review Session（生成時の対応表）

| 項目 | 保存する意味 |
|---|---|
| review_id・形式版 | 校正作業セットの識別 |
| run_id・Canonical manifest hash・元XDW hash | OCR原記録への参照。絶対パスだけを識別に使わない |
| ページ対応表 | page_id、元ページ、Reviewページ、用紙寸法、生成時回転角 |
| 領域の生成時情報 | 元region_id、生成アノテーションID、初期文字、**保存後に実測した位置・サイズ・向き** |
| 生成環境 | Core/Integrations/API版、文字サイズやフォント等の生成設定 |
| 初期XDW hash | 編集開始前の状態の識別 |

元OCR矩形と、赤文字12ptで作ったテキスト枠は同じサイズとは限らない。移動やサイズ変更を調べるときは、生成直後の実測枠と校正後の実測枠を比較する。

XDW内には小さい識別情報を保存し、全文・比較用の情報は対応表に置く。対応表のハッシュをXDWに入れる場合、対応表に完成後XDWのハッシュも含めると循環参照になる。識別用の不変メタデータと、生成完了後のファイル一覧・ハッシュ記録は分ける。

### 5.3 Reviewed Result

正本は1文書のJSONまたは小さなbundleとし、JSONLはそこから作る派生出力を推奨する。JSONLだけでは文字0件のページが消えるため、ページ一覧を正本に持つ。

| 階層 | 主な項目案 |
|---|---|
| 文書 | schema/version、result_id、review_id、取り込み日時、取り込み元XDW hash、元文書参照、座標系、取り込み完了状態 |
| ページ | page_id、page番号、元ページ番号、width_mm、height_mm、rotation、items配列 |
| テキスト | item_id、text、ページ基準のx/y/width/height、回転角、縦書き/横書き |
| 任意の来歴 | 元OCR領域の参照、XDWに記録されていた由来、照合状態、診断 |

座標は左上原点、右方向+x、下方向+y、mm。内部計算はSDKの1/100mm整数を使い、保存時にmmへ変換すれば丸め誤差を抑えられる。

寸法の意味は**SDKが報告するテキストアノテーション枠の外接矩形**とする。印刷された文字のインク部分の範囲や、OCRの文字領域とは同一視しない。回転角を残しても、それだけで完全なViewer表示を再現できる形式にはならない。

item_idは取り込み結果内で一意に採番する。元region_id・SDKのhandle・列挙順をitem_idへ流用しない。取り込みをまたいだ永続的な項目同一性は、この初期設計では保証しない。

通常の検索・テンプレート処理はtext/page/geometryだけを読む。Canonicalや来歴の読み込みを必須にしない。取り込み時も、Session側に必要な情報が揃っていればCanonical全体との差分計算は不要。

### 5.4 来歴の状態

| 状態の意味 | 扱い |
|---|---|
| 記録された由来が対応表と一致 | 元領域への参照を保持。ただし全編集操作の追跡が証明されたという意味ではない |
| 属性なし | 由来不明。新規追加とは断定しない |
| 同じ由来が複数項目にある | コピー・分割等の候補として残す。1対1対応は未確定 |
| 属性破損・未知の由来・別reviewの属性 | 内容を結果から除かず、来歴に診断を付ける |
| 読取SDKエラー | 単なる属性なしと区別し、読取不能なら文書の確定を止める |

非テキストの親を無視してその子まで捨ててはいけない。付箋・グループを走査し、子のテキストを取得する必要がある。未知のコンテナーに対して正しい位置計算ができない場合は、黙って欠落させず未対応構造として報告する。

## 6. 取り込み・保存の流れ

1. 利用者が1文書のReview作業セットを選ぶ。フォルダー名だけで元文書を推測しない。
2. 保存済みXDWを同一ボリュームの専用一時領域へコピーし、コピー元とコピーのハッシュを照合する。
3. 読み取り専用で開き、文書ID・ページID・順序・寸法・ページ回転をSessionと照合する。
4. 全ページを再帰走査する。全テキストの現在の文字・実測枠・向きを取得し、親相対座標をページ座標へ直す。
5. 来歴を任意情報として照合する。重複・欠落等は診断に残し、文字の取り込みと分離する。
6. Reviewed JSON、必要なJSONL、取り込み元XDWの固定コピー、検証要約を新しい一時フォルダーに作る。
7. 再読込検証と入力の再確認後、フォルダー単位で新規の版として確定する。
8. 成功・失敗、ページ数、文字数、来歴が不明/重複の件数、保存先を日本語で表示する。

この方式は保存済みのスナップショットを確定する。Viewerで未保存の編集内容は対象外であり、ファイルハッシュ検査はViewerの編集セッション自体をロックするものではない。利用手順は「保存して閉じる→取り込み」とする。

最初のReview XDW、編集用の作業XDW、取り込み済みXDWは役割を分ける。利用者は作業XDWを上書き保存できる。取り込みは新しいresult_idで版を追加し、以前のReviewed Resultを上書きしない案を推奨する。容量増加は伴うが、将来の比較を再現できる。

同じXDW hashを再取り込みした場合は「同一の保存内容を取込済み」と案内できる。新しい結果を作るか既存結果を返すかは実装仕様として確定する。保存・取り込み成功は全文の人手確認を機械が証明するものではない。

## 7. 初期版の推奨境界と必要な実装

### 推奨する利用仕様

- OCR開始と取り込みは別BAT。取り込み単位は1文書。
- 元と同じページ構成・寸法の白紙XDWを1文書1ファイルで作る。認識0件のページも作る。
- 生成文字は赤を初期値とする。取り込み対象は色で限定しない。
- 文字の修正・追加・削除・移動・コピーを扱う。
- ページの追加・削除・並べ替え・回転・用紙寸法変更は初期版では未対応として検出する。
- グループ/付箋内の文字も対象にするなら、親相対座標の変換を必須にする。多段構造等の未検証組み合わせは検証後に対応範囲へ加える。
- 空文字や空白だけの項目は原状態として保持し、診断を付ける案を推奨。検索用の除外は別段階にする。
- 列挙順は読み順とみなさない。初期版は取得順を記録し、必要なら別フィールドに座標順を作る。段組・表・縦書きの意味的な読み順推定は範囲外。
- 文書/ページの識別不整合は取り込みエラー。任意の領域来歴の曖昧さは警告・診断。

### 実装する単位

1. **新Review生成**：Canonicalの全ページ確認、白紙作成、赤文字配置、Session・識別属性・生成直後実測情報の保存。
2. **新Reviewed読取**：全テキスト走査、親座標変換、実測枠と文字属性、任意来歴の取得。
3. **Reviewed保存/読込/JSONL**：新しい形式版・検証・不変の版保存・文書単位確定。
4. **起動と状態管理**：OCRジョブにReview工程を追加、別の取り込みBAT、成功/失敗/未処理を表示。
5. **日本語文書と配布確認**：使い方、範囲外操作、再取り込み、Portableへの収録。

現行Coreには必要なRaw APIが既にある。初期実装はIntegrations側の小さなSDK境界で構成できる見込みで、Coreの公開APIを拡張することを前提にしない。公開配布物の更新・公開は別工程。

生成の現行補助 `add_text()` は1領域ごとに文書を開いて保存する。全ページ・大量文字へそのままループ流用すると保存回数が増える。新生成では1つの作業文書を開いてまとめて追加し、最後に保存・閉じる・再読込する設計にする。性能値は未測定。

## 8. 実装時の受入確認

| 分類 | 必須ケース |
|---|---|
| 生成 | 1ページ、複数ページ、混在寸法、文字0件ページ、全ページ文字0件、同文同位置/同文別位置の領域 |
| 文字 | 日本語、Unicode補助文字、改行、空白、長文、新規追加、削除、全削除、空文字 |
| 位置 | 移動、サイズ変更、回転、縦書き、付箋/グループ、多段構造、ページ端やはみ出し |
| 来歴 | 編集維持、コピーによる重複、属性なし、属性破損、別reviewからのコピー |
| ページ識別 | 別文書、同寸法ページ入替え、コピー、削除、180度回転、文書/ページ属性欠落 |
| 保存 | 途中中断、出力衝突、同一内容の再取込、入力の同時変更、原本・Canonical・過去版の不変 |
| 起動/配布 | 日本語・空白パス、取消し、未保存/使用中の文書、Portable同梱Python、選択文書1件 |

### 残る未検証事項

- 新しい白紙の複数ページ文書をViewerで編集・保存・再表示したときの表示、編集性、新文書/ページ属性の保持。
- グループの多段化、付箋上のグループ等の組み合わせと、回転した子のページ座標の実表示照合。
- 大量領域の所要時間・メモリ・XDW容量、文字の重なりやすさ。12pt固定は読みやすさや枠内収容を保証しない。
- DocuWorks 9.1本体やPortable同梱環境での新フローの実行。今回のPython 3.13＋製品DLLの結果から一般化しない。
- ファイル使用中、途中中断、JSON/JSONL同時確定等の完成した取り込み処理としての試験。

これらは実装時の受入条件として残す。調査段階では主要な技術選択に必要な情報は揃っており、製品実装や公開完了を意味しない。

## 9. 将来の差分分析を追加するとき

1. Sessionの生成直後情報、Canonical、対象版のReviewed Resultと取り込み元XDWを読む。
2. 由来が一意に照合できる領域を比較する。文字は元文字と、移動/サイズは生成直後の実測枠と比較する。
3. 重複や由来なしは、文字・位置・周辺配置から候補を作り、推定と記録された対応を分ける。
4. 分割・結合は多対多の独立した対応表へ保存する。元データを書き換えない。
5. 曖昧な候補だけ人が確定する。

初期状態と最終状態だけでは、途中の編集順序や編集者は復元できない。元領域が消えたように見えても、IDを失った作り直しと削除は区別できない場合がある。細かい編集履歴が必要になった場合は別の記録方式が必要となる。
