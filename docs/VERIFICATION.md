# 検証記録

## 0.5.0 ローカル検証（2026-09-07）

- Integrationsはソース／配布wheelとも97件成功（実機3件を含む）。新規のOCR未導入Python 3.13環境で共通結果・複数ページ・バッチ53件成功、pip checkとCLI確認も成功。
- Coreはローカル環境で103件成功。CoreのPythonコードは前版wheelと同一。公開監査テスト53件、両パッケージのwheel/sdist監査とtwine checkは成功。
- 合成3ページXDW（A4縦・検出0件A4・A3横）を直下とサブフォルダに置き、破損文書を挟んで実GPU認識。直下は成功1・失敗1、再帰は成功2・失敗1で、破損後の文書も全ページ保存。正常文書の検出数は各1/0/1件、各バッチのモデル初期化は1回。
- 採用GPU環境はPaddle GPU 3.2.2 CUDA 12.9 / PaddleOCR 3.7.0 / PaddleX 3.7.2のまま。cuDNNはbuild 9.9.0 / runtime 90900で、版差異警告なし。
- 新規軽量環境の最終wheelからJSONL出力・マーカーdry-run・実保存・再オープンに成功。OCR関連ライブラリは未import。3ページ目のマーカー点列誤差は最大0.003256mm未満、原本と他ページ画像は不変。
- ビルド一時ファイルのWindows ACL問題は成果物の内容を変えず継承権限のファイルへ置き直して対応。過去のGPU環境と0.4.0成果物は保持。
- 新出力のViewer確認は未実施。実データ・環境の絶対パスを含む証跡はローカル納品物に保存し、GitHubへ含めていない。

以上はローカル検証結果です。GPU・DocuWorks・ViewerをGitHub-hosted CIで検証したとは扱いません。

## 0.4.0 ローカル検証（2026-09-07）

- 配布wheelのIntegrationsテスト82件成功（実機3件を含む）。Core DLL不要102件成功、端末固有1件は除外。公開監査テスト51件成功。
- CoreとIntegrationsのwheel/sdist計4件でtwine check・配布内容監査成功。リポジトリ外の新規Python 3.13環境でimport/CLI/pip check成功。
- A4縦・白紙A4・A3横の3ページをGPU認識し、検出数1/0/1件。全ページ約16.7秒。ページ1・3の非連続指定も成功。
- OCR未導入環境から3ページ目のマーカーを保存・再オープン。点列誤差は最大0.003256mm未満。原本と1・2ページの描画画像は不変。
- Core/公開監査テストの一時フォルダ権限問題は、ローカル限定の継承権限fixtureで回避。ライブラリ本体やCIの期待値は変更していない。
- Viewerの新規確認は未実施。実文書・OCR結果・個人環境パス付きログはローカル納品物だけに保持。

以上はローカル実測であり、GitHub Actionsの実機・GPU検証を意味しません。


## 取り込み確認（2026-09-06）

Core 1.0.0の23ファイル、Integrations 0.3.0の16ファイル（PythonとJSON Schema）は元のwheel格納内容とバイト一致しています。[元配布物の名前・サイズ・SHA-256と比較結果](evidence/import-provenance.json)を保存しました。ライブラリ実装・公開APIは変更していません。

今回のコマンド・測定結果は [集約時検証](evidence/consolidation.json) に記録します。ネットワークからのGPU依存・モデルの全取得、実GPU認識、実文書保存、Viewerは今回の再検証に含めません。

## 過去のCore 1.0.0記録（2026-09-05、継承）

ローカル正式成果物の検証報告書を確認しました。以下は今回再実行した件数ではありません。

| 項目 | 記録 |
|---|---|
| Python 3.10 / 3.11 / 3.12 / 3.13 × wheel/source | 8環境で各103契約試験成功 |
| 実機 | Python 3.11で117件成功、Standard属性89/89、保存・再open |
| ABI | 141関数、63構造体、342フィールドの公式SDK由来宣言と実測の整合 |
| Viewer 9/9 | 旧版の確認を継承した記録。新たな目視ではない |
| 追加の例外試験 | Python 3.10は2成功/1失敗、3.11は3成功 |

元package READMEにある102件という記述より、実測報告103件を採用します。全141関数の実行・全出力バッファの安全性を証明したものではありません。元の実機証拠一式は個人パス・fixture情報を含むため公開セットへ複製せず、ここでは集計として継承しています。既知不具合は[制約](LIMITATIONS.md)を参照してください。

## 過去のIntegrations 0.3.0記録（2026-09-05、継承）

| 項目 | 記録 |
|---|---|
| ソース / wheel | 各64件成功、失敗・skip 0 |
| OCR未導入環境 | 20件成功。JSONLとMarker実保存・再open成功 |
| 旧結果移行 | 23領域の文字列・順序・座標一致 |
| 新規GPU OCR | 23領域、13.750秒（描画・モデル読込み等を含む1試行） |
| Marker | 前版と計画一致、座標誤差最大0.003325mm、原本不変 |
| Viewer | 前版へのユーザー成功報告を継承、新規Viewer操作なし |

生成XDWが前版と同一だったという記録はありますが、修復警告・編集可否等の個別項目は未報告です。所要時間はベンチマーク保証ではありません。実文書・OCR文字列・元文書hash・環境パスを含むログは公開しません。

[source JUnit](evidence/inherited-0.3.0/source-tests.xml) / [wheel JUnit](evidence/inherited-0.3.0/wheel-tests.xml) / [軽量JUnit](evidence/inherited-0.3.0/lightweight-tests.xml) は元ログからhostname/file属性を除いた公開用コピーです。元の署名付き証拠やそのままの原本ではありません。
